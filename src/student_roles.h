#ifndef _STUDENT_ROLES_
#define _STUDENT_ROLES_

#include "data_io.h"

void checkIn(User &loginUser);
void viewCheckInResult(User &loginUser);
void viewScoreOfACourse(User &loginUser);
void viewSchedule(User &loginUser);

#endif